# 第五阶段表格交互与信息展示发布记录（2026-08-12）

## 发布快照

- 生产发布目录：`/opt/jdair-cha/releases/20260812212342-layout-redesign-phase5`
- 程序 SHA-256：`517bb0f26e7f56e79291d6391160c84df9a578288ce0cf545d82363e98d4afc2`
- 前一稳定版本：`/opt/jdair-cha/releases/20260812202309-layout-redesign-phase4`
- 改造范围：视频记录查询的页面布局和前端交互；保留全部既有接口、查询参数、数据结构和视频播放能力。

## 本阶段内容

1. **操作列靠右**
   - 表格改为填满记录区域，操作列固定在最后一列并右对齐。
   - “高清播放 / 添加 / 复制名称”紧贴表格右侧并保持横向操作。

2. **大小/时长与文件标题**
   - 大小/时长列增加可用宽度，内容保持单行显示。
   - 文件标题列增加默认宽度；当前记录文件名可完整显示。
   - 表头分隔线可手动拖动，后续长文件名可继续按需要加宽标题列。

3. **参考信息折叠摘要**
   - 折叠时只渲染一条横向摘要：城市、候选状态、机号/航班、航段、时间、匹配关系及候选数量。
   - 超出当前列宽的摘要采用省略号显示；完整摘要保留在悬停提示中。
   - 展开后仍显示原有全部候选参考信息；“展开全部参考信息 / 收起全部参考信息”保持可用。

4. **每列手动调整**
   - 全部 8 个表头右侧都有可拖动分隔线。
   - 拖动分隔线可调整相邻列宽，最后一列可直接调整；列宽会保存到当前浏览器本地存储。
   - 双击任意分隔线可恢复默认列宽。

5. **设备栏并发缓存稳定性**
   - 修复多个页面请求同时刷新设备目录缓存时可能出现的临时文件竞争。
   - 该修复不改变设备数据来源或接口返回，只避免侧栏偶发错误提示。

## 验证结果

- Python 编译、内嵌 JavaScript 语法检查通过。
- 与第四阶段相比，页面中的 API 路径集合完全一致。
- 实际登录后，视频记录查询加载 25 行，终端设备栏正常加载且无错误提示。
- 当前分辨率下表格宽度为 1541px，操作列宽 180px，右边界与记录区域右边界一致。
- 文件标题当前 25 行均满足 `scrollWidth <= clientWidth`，完整文件名直接可见。
- 大小/时长列为单行显示；折叠参考信息为单行摘要，宽度不足时使用省略号。
- 8 个列宽拖动柄已加载；实际拖动文件标题列后，相邻列宽同步调整并写入浏览器本地存储。
- “展开全部参考信息”可展开当前页 25 条参考信息，随后可全部收起。
- `/api/health`、`/api/dashboard`、`/api/devices`、`/api/summary`、`/api/records`、`/api/flights`、`/api/routine-tasks` 均为 HTTP 200。

## 数据备份与回滚

改造前完整备份仍保留：

`/opt/jdair-cha/backups/jdair-cha-before-layout-redesign-phase2-20260812-162937.tar.gz`

备份 SHA-256：

`5496fb64da2d7e3ad5c25dd2be5ddcb5219e56c8527b194606adb1210389c6cd`

如需回退第五阶段，请将 `current` 软链接切回第四阶段稳定版本并重启服务：

```bash
ln -sfn /opt/jdair-cha/releases/20260812202309-layout-redesign-phase4 /opt/jdair-cha/current
systemctl restart jdair-cha.service
```
