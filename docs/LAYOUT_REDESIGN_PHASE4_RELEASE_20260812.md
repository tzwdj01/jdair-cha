# 第四阶段布局发布记录（2026-08-12）

## 发布快照

- 生产发布目录：`/opt/jdair-cha/releases/20260812202309-layout-redesign-phase4`
- 程序 SHA-256：`78cf62a2514b49a09ab2ada08d87ac09de5bca110fa7757e8f5ae97b83759164`
- 前一版本（可快速回退）：`/opt/jdair-cha/releases/20260812193050-layout-redesign-phase3`
- 改造范围：仅 HTML/CSS/前端交互布局；既有数据接口和业务数据结构未改造。

## 本阶段内容

1. **辅助查询的信息页签**
   - 在“航班动态”“例行任务”之外增加“系统功能与版本”页签。
   - 点击后隐藏查询内容，版本说明在主内容区平铺显示，不再占用独立右侧栏。
   - 保留航班动态和例行任务的原有切换、查询接口和数据加载方式。

2. **视频记录查询紧凑化**
   - 移除“文件类型”列，保留设备、文件、大小/时长、时间、参考信息和操作。
   - 表格固定为 8 列、11px 字号、默认 40px 行高；文件标题单行省略，悬停可查看完整文件名。
   - 参考信息默认单行，单条“展开”和全局“展开全部参考信息/收起全部参考信息”均可用。
   - 高清播放、添加、复制名称保持同一横向操作行。

3. **参考信息批量操作**
   - 平台文件、设备文件和播放窗口右侧增加“展开全部参考信息”。
   - 已匹配的当前页参考信息可一键全部展开或收起；不影响视频播放和播放列表。

4. **终端设备栏高密度显示**
   - 设备主信息、状态、城市/仓库和历史信息改为更紧凑的两行结构。
   - 设备项字号统一为 12px、默认高度 40px，便于在同一屏显示更多终端。

## 验证结果

- 静态检查：Python 编译通过；内联 JavaScript 语法检查通过；后端逻辑与第三阶段一致。
- 生产服务：`jdair-cha.service` 为 active，根页面 HTTP 200。
- 接口回归：`/api/health`、`/api/dashboard`、`/api/devices`、`/api/summary`、`/api/records`、`/api/flights`、`/api/routine-tasks` 均返回 HTTP 200。
- 浏览器回归：辅助查询三页签切换正常；信息页签使用主内容区；视频表格、单条展开、全部展开/收起、文件名复制和设备栏紧凑样式均已验证。

## 数据备份与恢复

改造前完整备份仍保留在：

`/opt/jdair-cha/backups/jdair-cha-before-layout-redesign-phase2-20260812-162937.tar.gz`

备份 SHA-256：

`5496fb64da2d7e3ad5c25dd2be5ddcb5219e56c8527b194606adb1210389c6cd`

如需回退当前第四阶段发布，可将 `current` 软链接切回上一版本目录并重启 `jdair-cha.service`。
